#include <stdio.h>
#include <stdbool.h>  // Inclui a biblioteca para usar booleanos

int main() {
    int idade, num2;
    float salario = 7500;      // Declara salário como float
    double qtd_atomos;         // Declara a quantidade de átomos como double
    bool confirma = false;     // Declara uma variável booleana
    char genero = 'M';         // Usa aspas simples corretas para 'M'

    return 0;
}
